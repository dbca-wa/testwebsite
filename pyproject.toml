[project]
name = "testwebsite"
version = "0.1.0"
description = "Minimal testing website"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bottle>=0.13.4",
    "gunicorn>=25.1.0",
]
